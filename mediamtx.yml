# MediaMTX 至少需要一条路径规则才能接收 RTSP 发布。
moq: false
rtmp: false
webrtc: false
srt: false

api: true
apiAddress: 127.0.0.1:9997

writeQueueSize: 2048

# HLS 只保留实时观看所需的短窗口。
hlsAlwaysRemux: true
hlsVariant: fmp4
hlsSegmentDuration: 1s
hlsSegmentCount: 7

paths:
  all_others:
