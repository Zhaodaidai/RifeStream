# MediaMTX 至少需要一条路径规则才能接收 RTSP 发布。
moq: false

# 保留短 fMP4 窗口；完整时间轴跳转由 playback.py 重启转码实现。
hlsAlwaysRemux: true
hlsVariant: fmp4
hlsSegmentDuration: 1s
hlsSegmentCount: 30
hlsDirectory: ./.hls

paths:
  all_others:
